define(['add', 'subtract', 'multiply', 'divide'], function(add, subtract, multiply, divide) {
    return {
      add: add,
      subtract: subtract,
      multiply: multiply,
      divide: divide
    };
});
